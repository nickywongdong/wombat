// Axolotl Testing Apparatus for Both Daemons

#include <cstdio>
#include <cstdlib>
#include <iostream>
#include <pthread.h>

int dcdpid = -1;
int dldpid = -1;

int main() {

  // Compile, Fork, and Exec Dcd
  // clear old versions of dcd

  // compile

  // compile check: if executable exists, fork and exec.
  // otherwise, print error.

  // Compile, Fork, and Exec Dld

  return 0;
}
